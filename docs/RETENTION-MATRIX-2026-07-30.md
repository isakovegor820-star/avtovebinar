# Матрица сроков хранения АСПБ

Версия: `2026-08-23.15`
Дата утверждения в коде: 21.08.2026
Статус: локальная реализация; до production требуется проверка российского юриста, DPO и письменное утверждение руководителя.

HMAC IP, Telegram ID и `visitorId` являются псевдонимизированными персональными данными, а не автоматически обезличенными. Legal hold, инцидент, спор, действующий договор и прямая обязанность по закону приостанавливают уничтожение только в документированном объёме. Существующие журналы инцидентов не удаляются.

| Цель | Данные | Таблица/носитель | Срок | Начало срока | Удаление/анонимизация | Исключение | Доказательство |
|---|---|---|---|---|---|---|---|
| Регистрация и доступ | имя, телефон, email, город, статус | `leads`, `registrations` | 3 года | последнее взаимодействие/обновление | ПДн лида заменяются нейтральными значениями, связи для статистики сохраняются | активный договор, спор, закон, legal hold | `retention_runs`, audit действия по запросу |
| Tenant CRM-контакт | Organization, нормализованные email/телефон, display name, source, manager, next contact, current pipeline/stage, legacy Lead link | `crm_contacts` | **не утверждён**; до решения юриста/DPO — без автоудаления и не дольше обоснованного срока связанного `lead` | последнее CRM-взаимодействие, закрытие договорного интереса или tenant erasure request | только утверждённый tenant-scoped erasure/anonymization process; contact projection должна быть синхронизирована с анонимизацией Lead, не раскрывая другой tenant | действующий договор, спор, закон, legal hold | composite tenant FK/unique, masked analyst projection, cross-tenant integration/postflight |
| CRM-задача и напоминание | Organization/CRMContact, tenant assignee membership, title/description, priority/status, due/reminder, completion/cancellation timestamps, creator | `crm_tasks`, task events в `crm_contact_events` | **не утверждён**; до решения юриста/DPO — без автоудаления | завершение/отмена задачи, закрытие контакта или tenant erasure request | физический delete запрещён DB trigger; до утверждённого tenant-scoped erasure process задача только получает terminal status, а текст должен минимизировать ПДн | договор, спор, проверка, legal hold | composite tenant FK, active assignee/lifecycle DB checks, event/audit и queue negative tests |
| CRM scoring и ручной hot | Organization/CRMContact, версия/правила, immutable факторы с source entity и dedup key, вычисленный балл, reason/actor/source ручного решения | `crm_scoring_rule_sets`, `crm_scoring_rules`, `crm_score_factors`, score/manual-hot поля `crm_contacts`, события в `crm_contact_events` | **не утверждён**; до решения юриста/DPO — без автоудаления | последнее подтверждённое событие-фактор, смена версии/ручного решения, закрытие контакта или tenant erasure request | фактор не редактируется и не удаляется обычным application path; до утверждённого tenant-scoped process version/factor history сохраняется для объяснимости, а удаление source data не выполняется каскадно импровизированным SQL | договор, спор, проверка модели, legal hold | tenant FK, immutable trigger, factor dedup, version activation/manual override audit, migration postflight и integration tests |
| Tenant CRM-теги | Organization, normalized/display name, color token, status, contact assignment и actor/time | `crm_tags`, `crm_contact_tags`, tag events в `crm_contact_events` | **не утверждён**; до решения юриста/DPO — без автоудаления | archive тега, снятие с последнего контакта, закрытие контакта или tenant erasure request | использованный тег физически не удаляется; archive сохраняет смысл исторических назначений, tenant erasure — только отдельным утверждённым процессом | договор, спор, legal hold | tenant composite FK/unique, physical-delete guard, safe cross-tenant API и audit tests |
| CRM bulk preview и результат | Organization, requester membership, action payload, только названия применённых фильтров без search/value, точный snapshot contact IDs до 1000, idempotency/request hash, safe per-contact result codes, timestamps | `crm_bulk_actions`; связанные bulk-задачи в `crm_tasks`; summary в `audit_logs` | **не утверждён**; preview исполняется только 10 минут, но row/history до решения юриста/DPO автоматически не удаляется | expiry preview, terminal execution, закрытие контактов или tenant erasure request | expiry запрещает выполнение, но не удаляет доказательство; action/task description должны содержать минимум ПДн; очистка snapshot/results разрешена только будущим утверждённым tenant-scoped process вместе с audit/legal hold | договор, спор, проверка массового действия, legal hold | immutable snapshot/terminal result, result-integrity checks, tenant FK, exact-count preview, partial-result integration и postflight |
| CRM CSV export | текущая tenant-scoped contact projection; для ANALYST/AUDITOR — маскированная | одноразовый HTTP `text/csv` response; сам CSV в БД/object storage/browser storage не сохраняется; только summary audit без contact IDs/ПДн | файл живёт только в ответе/локальной загрузке пользователя; audit — по сроку административного аудита | завершение HTTP response; для пользовательской копии — локальная политика получателя | server не имеет retained-файла для cleanup; получатель обязан хранить/удалять скачанную копию по утверждённой политике ПДн | спор, проверка, legal hold относятся только к минимальному audit, не создают серверную копию CSV | explicit membership permission, `private, no-store`, row cap 10000, masking, formula-injection escape и export audit |
| Tenant CRM marketing delivery | Organization/CRMContact, exact Registration/Webinar/WebinarSession, канал, тема email/текст сообщения, consent evidence ID, requester membership, status/attempts/safe error/timestamps; recipient email и Telegram chatId в job не сохраняются | `crm_deliveries`; минимальные status events в `crm_contact_events` и `audit_logs` без message/address/chatId/provider details | **не утверждён**; до решения юриста/DPO — без автоудаления | terminal `sent`/`blocked`/`dead_letter`/`cancelled`, отзыв согласия, закрытие контакта или tenant erasure request | только будущий утверждённый tenant-scoped cleanup, согласованный с consent/audit/legal hold; обычный API разрешает status/retry, но не физический delete | спор о согласии/доставке, договор, инцидент, закон, legal hold | enqueue и немедленный pre-send consent recheck под channel lock, tenant FK/trigger, safe projection, retry/dead-letter integration/postflight, aggregate metrics |
| CRM pipeline/stage и история переходов | названия/code/category этапов, actor User, before/after, reason, source, correlation ID, server time | `crm_pipelines`, `crm_stages`, `crm_stage_transitions`, `crm_contact_events` | **не утверждён**; до решения юриста/DPO — без автоудаления | archive stage/pipeline, закрытие контакта или tenant erasure request | protected/used stage физически не удаляется; отдельный утверждённый process должен сохранить бизнес-смысл/audit и минимизировать actor/contact references | договор, спор, проверка, legal hold | DB trigger, `audit_logs`, migration postflight и transition negative tests |
| Избранное зрителя | User, Organization, Webinar, время сохранения | `viewer_webinar_favorites` | **не утверждён**; до решения юриста/DPO — без автоудаления | ручное удаление favorite, закрытие аккаунта или tenant erasure request | пользователь удаляет отдельный favorite немедленно; массовое удаление — только tenant/account-scoped erasure process | спор/legal hold только для документированного набора | same-User/tenant API, idempotency и private-access negative tests |
| Прогресс просмотра | User, Organization, Webinar, WebinarSession, position/duration, dedup key, timestamps | `viewer_webinar_progress` | **не утверждён**; до решения юриста/DPO — без автоудаления | последнее наблюдение, закрытие доступа или account erasure request | tenant/account-scoped cleanup после утверждения срока; browser storage и отдельные progress events не создаются | спор, договор, legal hold | unique scope, server throttle/dedup, background-tab/replay acceptance |
| Личные заметки зрителя | User, Organization, Webinar, WebinarSession, таймкод, текст | `viewer_webinar_notes` | до удаления пользователем; общий максимальный срок **не утверждён** | создание/последнее изменение или account erasure request | отдельная заметка удаляется пользователем; массовое удаление только same-User/tenant erasure process | документированный legal hold/спор | owner-only API, safe cross-tenant 404, delete/browser tests |
| Настройки уведомлений зрителя | User, Organization, отдельные marketing/service channel flags | `viewer_notification_preferences` | **не утверждён**; до решения юриста/DPO — вместе с account relationship, без автоудаления | изменение настройки, закрытие аккаунта или tenant erasure request | tenant/account-scoped cleanup; consent history не изменяется и следует отдельному сроку `consent_records` | обязанность сохранить доказательство согласия/отзыва, спор/legal hold | read-only GET, explicit PATCH, consent grant/revoke evidence tests |
| Токены доступа | хэш токена, назначение, регистрация | `registration_tokens` | срок действия + 7 дней | `expires_at` | удаление строки | расследуемый инцидент с документированным legal hold | `retention_runs.result_json` |
| Passwordless-токены User | SHA-256 хэш, purpose, expiry, consumed timestamp | `user_auth_tokens` | срок действия + 24 часа | `expires_at` | удаление строки worker cleanup | документированный security legal hold | worker log/metrics |
| User session | SHA-256 хэш, active organization, IP-hash, User-Agent, revoke/expiry | `user_sessions` | expiry + 30 дней | `expires_at` | удаление строки worker cleanup | расследование инцидента/legal hold | audit + queue health |
| Passwordless email outbox | User reference, state, attempts, redacted error; без email и raw URL | `user_auth_email_jobs` | 90 дней после terminal status | `updated_at` | удаление `sent/cancelled/dead_letter` job | спор/инцидент/legal hold | protected health + Prometheus metrics |
| Организационное приглашение | organization, нормализованный email, роль, статус, инициатор/акцептор | `organization_invitations` | 3 года | принятие, отзыв или истечение | отдельный утверждённый tenant/account cleanup; audit сохраняется по своей политике | спор, инцидент, legal hold | `audit_logs`, tenant-scoped API |
| Токен приглашения | SHA-256 хэш, invitation, expiry, consume/invalidate timestamp | `organization_invitation_tokens` | срок действия + 7 дней | `expires_at` | удаление строки worker cleanup | документированный security legal hold | worker cleanup result |
| Invitation email outbox | invitation reference, state, attempts, redacted error; без raw URL | `organization_invitation_email_jobs` | 90 дней после terminal status | `updated_at` | удаление `sent/cancelled/dead_letter` job | спор/инцидент/legal hold | protected health + Prometheus metrics |
| Private Webinar access grant | organization, Webinar, HMAC email, optional User reference, purpose, expiry/revoke/accept timestamps | `webinar_access_grants` | **не утверждён**; до утверждения — без автоудаления grant history | отзыв, истечение или закрытие Webinar | только отдельный утверждённый tenant/account erasure process; audit сохраняется по своей политике | спор, инцидент, legal hold | `audit_logs`, tenant-scoped API, negative integration tests |
| Private Webinar invitation token | SHA-256 hash, grant, expiry, consume/invalidate timestamp | `webinar_access_grant_tokens` | срок действия + 7 дней | `expires_at` | удаление строки worker cleanup | документированный security legal hold | worker cleanup + integration test |
| Private Webinar invitation outbox | grant reference, email, state, attempts, redacted error; raw URL не хранится | `webinar_access_invitation_email_jobs` | 90 дней после terminal status | `updated_at` | удаление `sent/cancelled/dead_letter` job | спор/инцидент/legal hold | protected health, Prometheus metrics, integration test |
| MFA владельца | зашифрованный TOTP secret, enabled/enrollment timestamps, session verification timestamp | `users`, `user_sessions` | до отключения MFA/удаления аккаунта; session marker — вместе с session | включение/отключение MFA, expiry session | secret удаляется при отключении; session — expiry + 30 дней | security incident/legal hold | `audit_logs`, authentication tests |
| Профиль автора и решение о проверке | публичные профессиональные сведения, статус, комментарий автору, внутренняя причина | `author_profiles`, `author_verifications` | **не утверждён**; до утверждения — без автоудаления | закрытие аккаунта/прекращение публикации | отдельный tenant/account erasure после письменного решения юриста/DPO; обязательный аудит сохраняется отдельно | спор, проверка, legal hold | `audit_logs`, tenant/public projection tests |
| Документы проверки автора | минимально необходимые PDF/JPEG/PNG, MIME, size, checksum | `author_verification_evidence` (приватное хранение) | **не утверждён**; до утверждения — без автоудаления | финальное решение о проверке/закрытие аккаунта | неотправленный документ автор удаляет сам; остальное удаление — только утверждённым erasure process | спор, проверка, legal hold | access audit, `no-store`/`noindex`, private projection tests |
| Вопросы | текст, выбранная публикация | `questions` | 1 год | `created_at` | текст и отображаемое имя заменяются/очищаются | незавершённый ответ, спор, legal hold | `retention_runs` |
| Чат участника | exact Organization/Webinar/WebinarSession/Registration, server message type, имя/псевдоним, сообщение; synthetic сообщения не входят в participant cleanup | `webinar_chat_messages` | 1 год | `created_at` | удаление только participant-сообщения с сохранением минимального audit по его отдельной политике | legal hold | exact tenant/session DB guards, `retention_runs`, integration/postflight |
| Подготовленный чат | сценарный текст без данных участника, версия, тип, таймкод, `draft/approved/rejected`, synthetic-маркировка | `chat_scenarios`, `chat_scenario_messages`, legacy `webinar-data` | срок актуальности сценария и публикации Webinar | публикация/архивирование версии сценария | draft заменяется новой версией; published history удаляется только утверждённым content-erasure process | авторские и доказательные материалы, спор/legal hold | approval/status DB guards, audit, tenant integration, Git для legacy scenario |
| Модерация чата | exact Organization/Webinar/WebinarSession/Registration, hidden/blocked timestamps, причина, actor membership, revision и before/after audit | moderation fields в `webinar_chat_messages`/`registrations`, `audit_logs` | **не утверждён**; до решения юриста/DPO — без автоудаления | восстановление доступа, архивирование Webinar или tenant erasure request | обычный API только меняет moderation state; физическое удаление до утверждённого tenant-scoped process запрещено | жалоба, спор, инцидент, legal hold | mandatory reason, actor/tenant scope, optimistic revision, audit и negative integration tests |
| Очередь и grounded-модерация вопросов | exact tenant/Webinar/Session/Registration, normalized grouping fingerprint, status/priority/revision, reason/actor/source/correlation, AI provenance, основанный черновик, citation и human-review state; текст вопроса остаётся в `questions` | `questions`, `question_moderation_events`, `ai_operation_provenance`, `ai_suggestions`, минимальные `crm_contact_events`/`audit_logs`; опубликованный ответ — `webinar_chat_messages` | **не утверждён**; до решения юриста/DPO — без автоудаления moderation/provenance history; текст вопроса следует утверждённому сроку строки «Вопросы» | terminal review/status, archive Webinar, participant/tenant erasure request | обычный API не удаляет history и не автопубликует suggestion; будущий tenant-scoped cleanup обязан синхронизировать question, CRM/audit references и legal hold, не сохраняя provider request/response | жалоба, спор, проверка AI-основания, инцидент, legal hold | published-only retrieval, policy/handoff tests, human review, tenant DB scope, CRM/history/audit transaction и migration postflight |
| Media source, renditions и multipart metadata | original filename, MIME, expected/actual checksum, container/codecs/dimensions, private source/HLS/poster/OGG keys, processing status/version/lease | `media_assets`, `media_uploads`, `media_jobs`; выбранный private persistent volume, optional approved S3 | **не утверждён** для READY/published assets; incomplete upload живёт не более 24 часов в application contract | upload expiry, replacement/unpublish или archive Webinar | worker удаляет expired local multipart directory либо вызывает provider Abort и отменяет metadata/job; external S3 lifecycle остаётся обязательным вторым барьером при его выборе. Active/published source, HLS/poster/audio удаляются только утверждённым tenant/content erasure flow; backup media volume следует той же policy после её утверждения | авторские права, спор, legal hold | `audit_logs`, checks `20260821120000`–`20260821122000`, local cleanup/integrity tests; provider deletion receipt только для внешнего S3 |
| Временный STT provider result | private speech rendition URI, распознанные segments/timestamps/speaker labels, operation ID у provider | Yandex SpeechKit либо approved alternative; operation ID не является публичным API | provider docs указывают result availability до 3 дней; договорный срок logs/backups **не утверждён** | async recognition completion/failure | adapter вызывает provider `deleteRecognition` сразу после чтения; неподтверждённое удаление fail closed. OGG source удаляется только вместе с MediaAsset по утверждённому erasure flow | incident/legal hold только по письменно согласованной provider процедуре | provider delete outcome, safe job error, DEC-06 acceptance |
| Транскрипт, словарь и производный контент | segments/speaker/timestamps/text, organization terms, chapters, tags | `transcripts`, `transcript_segments`, `organization_terms`, `webinar_chapters`, `webinar_tags` | **не утверждён**; до решения юриста/DPO — без автоудаления published/history | archive/unpublish Webinar или tenant erasure request | удаление только tenant-scoped content-erasure process с согласованной политикой published versions/search index; viewer snapshot и WebVTT вычисляются из current published version, отдельно не сохраняются, captions URL не хранится | авторские права, спор, legal hold | audit, transcript versions, postflight/search/captions negative tests |
| AI provenance и suggestions | task/provider/model/template version, input entity references/revisions, generated suggestion, human review state | `ai_operation_provenance`, `ai_suggestions`, `content_jobs` | **не утверждён**; до DPO/data-processing review — без автоудаления | terminal job/review или archive Webinar | approved tenant/content erasure; provider request/response bodies, secrets и signed URLs не хранятся | спор о publication/review, incident, legal hold | provenance row, review audit, DEC-06 provider records |
| Партнёрская заявка | поля заявки, статус, комментарий | `partner_applications` и связанный `lead` | активная заявка; ПДн лида — до 3 лет после завершения взаимодействия | последний статус/контакт | анонимизация лида; активные/договорные записи не чистятся автоматически | договор, бухгалтерские/налоговые сроки, спор | audit + `retention_runs` |
| Email outbox | адрес, имя, персональные URL, ошибка | `email_outbox_jobs` | 90 дней после terminal status | `updated_at` при `sent/cancelled/dead_letter` | удаление job | расследование доставки/инцидента | `email_outbox_dead_letters`, `retention_runs` |
| Email dead letter | job ID, тип, число попыток, причина без адреса/URL | `email_outbox_dead_letters` | 3 года | `created_at` | удаление по отдельному утверждённому процессу | спор/инцидент | системный журнал |
| Telegram-профиль | Chat ID, username, имя | `leads` | 1 год без активности при отсутствии согласия и активной регистрации | `telegram_subscribed_at` | очистка идентификаторов | активные организационные сообщения/договор | `retention_runs` |
| Tenant manager Telegram binding | Organization/membership, HMAC chat hash, encrypted-at-rest boundary отсутствует: raw chat ID нужен только transport worker, status/claim/confirm/revoke timestamps; одноразовый raw token не хранится | `telegram_manager_chat_bindings`, `telegram_manager_chat_binding_tokens` | **не утверждён**; до решения DPO — без автоудаления history | revoke/expiry/deactivation membership | будущий tenant-scoped cleanup должен удалить raw chat ID и истёкший token hash, сохранив минимальный audit; текущий revoke немедленно запрещает callbacks, но физически history не удаляет | спор о manager action, incident, legal hold | OWNER confirm/revoke audit, hash-only token, DB scope/immutability guards, negative integration |
| Telegram consultant request | HMAC chat hash, provider message ID/key, исходный нормализованный текст, original/corrected topic-intent-urgency, model/version и exact tenant scope при однозначной registration | `telegram_consultant_messages`; analytics/CRM projection без текста в `telegram_bot_events`, `events`, `crm_contact_events` | **не утверждён**; до legal/DPO review — без автоудаления | resolved request, participant/tenant erasure request | только отдельный approved cleanup исходного текста/chat hash; original classification и минимальный review audit сохраняются по своей утверждённой политике | незавершённая передача человеку, спор, incident, legal hold | immutable original fields, reasoned correction audit, no-text analytics integration/postflight |
| Tenant Telegram broadcast | approved template text/version, exact preview scope/count/snapshot/hash-only token, job/recipient delivery status, consent evidence, raw chat ID только в private recipient transport row, correlation/provider message ID; signed room URL не хранится | `telegram_broadcast_templates`, `telegram_broadcast_previews`, `telegram_broadcast_jobs`, `telegram_broadcast_recipients` | **не утверждён**; preview допускает confirm только 10 минут, но физическая retention history до DPO решения не автоматизирована | preview expiry/consume; job terminal sent/cancelled/dead-letter; consent revoke | будущий approved tenant cleanup сначала очищает raw recipient chat IDs и expired preview hashes, сохраняя минимальный delivery/audit; active/pending job не удаляется | delivery/consent dispute, incident, legal hold | exact tenant/session triggers, preview hash/snapshot, pre-send consent recheck, pause/cancel/retry/dead-letter integration/postflight |
| Telegram bot event | tenant/Webinar/Session/registration/contact/membership references, direction/type/status, correlation ID, provider message ID и безопасные metadata без token/signed URL/email/phone/raw chat ID | `telegram_bot_events` | **не утверждён**; до observability/DPO review — без автоудаления | terminal interaction/delivery или erasure request | отдельный controlled cleanup после сверки delivery/audit/legal hold; append-only API не предоставляет delete/update | support/delivery dispute, incident, legal hold | immutable DB trigger, exact composite scope triggers, PII/token negative tests |
| Доказательства согласия | версия/хэш текста, цели, данные, операции, канал, IP-hash, UA, отзыв | `consent_records`, `legal_acceptances` | 3 года после прекращения соответствующей обработки/отзыва | событие отзыва/прекращения | append-only; уничтожение только отдельным утверждённым процессом | спор, проверка, legal hold | immutable DB trigger, backup, выборка |
| Visitor ID и подробные события | Visitor ID, страница, UTM, IP-hash, UA, metadata | `events`, cookie | 180 дней | событие/установка cookie | удаление события; cookie истекает/удаляется при отказе | агрегаты без идентификаторов могут храниться дольше | `retention_runs`, HTTP cookie test |
| UTM лида | source/UTM | `leads` | 180 дней | `updated_at` | очистка UTM/source | необходимая договорная атрибуция с документированным основанием | `retention_runs` |
| IP-hash/User-Agent админ-аудита | IP-hash, UA | `audit_logs` | 180 дней для технических меток | `created_at` | поля очищаются, факт действия остаётся | расследование инцидента/legal hold | `retention_runs` |
| Административный аудит | актор, действие, объект, before/after без лишних ПДн | `audit_logs` | 3 года | `created_at` | отдельное контролируемое удаление после проверки legal hold | спор, инцидент, проверка | audit export |
| Cookie выбора | `aspb_cookie_consent` | браузер | 12 месяцев | решение пользователя | удаление пользователем/истечение | нет | browser test |
| CSRF/session cookie | CSRF, participant/admin/User session | браузер и hashes в БД | 12 часов / 7 дней / 24 часа; User session 7 дней | выдача/последнее использование | истечение, revoke и удаление hash | security incident/legal hold | HTTP/integration tests |

Автоматический job реализован в `src/lib/retention.ts`. Каждый запуск сначала создаёт `retention_runs(status=running)`, затем фиксирует `completed` с количеством изменённых строк либо `failed` с ошибкой. Значения нельзя считать окончательно утверждёнными до юридической и бизнес-проверки матрицы.
